export default function ComingSoon({ title }) {
  return (
    <div className="card dash-soon">
      <h1>{title}</h1>
      <p className="muted">This module is in the admin navigation. Use Dashboard for live operations, then return here as each tool is finished.</p>
    </div>
  );
}
